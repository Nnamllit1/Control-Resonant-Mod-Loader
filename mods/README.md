# Local mod packages

Place each package in its own directory containing `mod.ini` and a `.wasm` module.
Local packages are ignored by Git. Maintained examples live in `examples/`.
The distribution uses `dist/crml/mods/`; the standalone host accepts either path.
